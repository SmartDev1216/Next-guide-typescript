import { Metadata } from "next"

export const metadata:Metadata = {
    title:'customers'
}

export default function Customers() {
    return (
        <div>
            <p>
                Customers page
            </p>
        </div>
    )

}